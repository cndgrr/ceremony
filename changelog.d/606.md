### Fixed

- `refs-not-closing` shows the `Refs` occurrence that produced the
  intersection rather than a correct `Closes` line, and gives an
  all-quoted intersection its own remedy: de-parse the archived record's
  tokens and leave the closing sentence alone (#606).
